* install.sh: create '.excludes' file - all files listed are excluded from linking
* install.sh: create '.dirs' file - all dirs listed have their children linked but the directory is created at destination
* install.sh: make '.config' a special case so that only subdirectories get aliased, not '.config' itself
* install.sh: ignore "Icon\r"
* install.sh: colorize output
* aliases: move to ~ and make compatible with fish, bash, zsh so they all use the same aliases file
* make grc happen
