#!/bin/bash
# Update fish shell

if installed fish; then
  echo "Updating fish shell..."
  curl -Lo ~/.config/fish/functions/fin.fish https://git.io/fin
  fin
else
  echo "fish shell not found, skipping"
fi
