#!/bin/bash
# Update ruby gems

if installed gem; then
  echo "Updating ruby gems..."
  installed update_rubygems || gem install rubygems-update
  update_rubygems
  gem update
  gem clean
else
  echo "gem not installed, skipping"
fi
